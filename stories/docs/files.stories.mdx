import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Documentation/Files" />

# Files

## getFilesWithKeyword(keyword)

It returns the file object from a keyword

```javascript
getFilesWithKeyword('myKeyword').then(file => console.log(file))
```