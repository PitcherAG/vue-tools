import { Meta, Story, ArgsTable, Preview } from '@storybook/addon-docs/blocks';

import FileCard from '@/components/FileCard/FileCard'

<!-- Story args -->
<!-- https://storybook.js.org/docs/react/writing-stories/args -->
export const args = {
    body: 'very long name of the file',
    category: 'presentation',
    imgUrl: 'https://btocpitcher.s3.amazonaws.com/thumbs/249549_1509373331.png',
    keywords: '',
    date: new Date().toDateString(),
    isNew: true,
    isFavorite: false,
    fileOptionsItems: []
}

<!-- Story parameters -->
<!-- https://storybook.js.org/docs/react/writing-stories/parameters -->
export const parameters = {
    docs: { 
        source: { 
            code: 'Some custom code here...'
        }    
    }
}

<!-- https://storybook.js.org/docs/react/api/argtypes -->
export const argTypes = {
    body: {
        /* Add v-model description to the argsTable */
        'v-model': {
            description: 'v-model description here',
            type: { required: true },
            table: {
                // defaultValue: { summary: false },
                type: { summary: 'boolean' }
            }
        },
        /* Disable an arg from appearing in the argsTable */
        value: {
            table: { 
                disable: true
            },
        },        
        type: { name: 'string', required: true },
        description: 'This is a custom description for the body, you can use `markdown` here'
    }
}

<!-- Named exports of stories here -->
export const Template = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { FileCard },
    template: '<FileCard v-bind="$props" />'
})

<!-- Define story settings here -->
<Meta 
    title="Components/FileCard"
    component={FileCard}
    argTypes={argTypes}
    args={args}
/>

# FileCard

## Custom Card component for files

Description and useful things to know for the component go here...

<!-- Preview withToolbar adds the toolbar section which includes the Show code button -->
<Preview withToolbar>
    <Story 
        name="standard"
        args={{ body: 'Standard' }}
        parameters={parameters}
    >
        {Template.bind({})}
    </Story>
</Preview>


### Sizes

Some variations of the component might go here...

<Story name="sizes" args={{ body: 'Sizes' }}>
    {Template.bind({})}
</Story>

### Arguments table

<!-- https://storybook.js.org/docs/react/writing-docs/doc-blocks#customizing -->
<ArgsTable story="standard" />

### Code examples

<!-- Check if we can use include here -->
```html
<FileCard v-bind="$props" />
```
