import { Meta, Story, ArgsTable, Preview } from '@storybook/addon-docs/blocks';

import FileCard from '../FileCard/FileCard'

<Meta 
    title="Components/FileCard"
    component={FileCard}
    argTypes={{}}
    args={{
        body: 'very long name of the file',
        category: 'presentation',
        imgUrl: 'https://btocpitcher.s3.amazonaws.com/thumbs/249549_1509373331.png',
        keywords: '',
        date: new Date().toDateString(),
        isNew: true,
        isFavorite: false                
    }}
/>

# FileCard

## Custom Card component for files

Description and useful things to know for the component go here...

<Story
    name="standard"
    args={{ body: 'Standard' }}
>
    {Standard.bind({})}
</Story>


### Sizes

Some variations of the component might go here...

<Story
    name="sizes"
    args={{ body: 'Sizes' }}
>
    {Sizes.bind({})}
</Story>

### Playground

Experiment with this story with the Controls addon in the Canvas tab.

<Canvas>
    <Story
        name="controls"
        args={{ fileOptionsItems: [] }}
        >
        {Controls.bind({})}
    </Story>
</Canvas>

### Arguments table

<ArgsTable of={FileCard} />

<!-- Stories named exports here -->
export const Standard = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { FileCard },
    template: '<FileCard v-bind="$props" />'
})

export const Sizes = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { FileCard },
    template: '<FileCard v-bind="$props" />'
})

export const Controls = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { FileCard },
    template: '<FileCard v-bind="$props" />'
})
