import { Meta, Story, ArgsTable, Preview } from '@storybook/addon-docs/blocks';

import Checkbox from '@/components/Checkbox'

<!-- Story args -->
<!-- https://storybook.js.org/docs/react/writing-stories/args -->
export const args = {
    value: false
}

<!-- https://storybook.js.org/docs/react/api/argtypes -->
export const argTypes = {
    value: {
        description: '`v-model` model to track checkbox status',
    },
    label: {
        description: 'checkbox label text'
    },
    radio: {
        description: 'radio style checkbox'
    },
    toggle: {
        description: 'toggle style checkbox'
    },
    slider: {
        description: 'slider style checkbox'
    },
    readonly: {
        description: 'readonly checkbox'
    },
    disabled: {
        description: 'disable checkbox'
    },
    fitted: {
        description: 'remove the padding for the input box. Useful when you dont have a label to have equal spacing'
    },
    uncheckable: {
        description: 'make the checbox uncheckable. Can be useful for radio style checboxes'
    },
    inverted: {
        description: 'inverts the color of checkbox'
    },
    size: {
        description: 'mini | tiny | small | medium | large | big | huge | massive'
    }    
}

<!-- Named exports of stories here -->
export const Template = (args, { argTypes }) => {
    return {
        props: Object.keys(argTypes),
        components: { Checkbox },
        template: '<Checkbox v-bind="$props" />'
    }
} 

<!-- Define story settings here -->
<Meta 
    title="Components/Checkbox"
    component={Checkbox}
    argTypes={argTypes}
    args={args}
/>

# Checkbox
Displays a fomantic checkbox

<Story name="Default">
    {Template.bind({})}
</Story>

## Disabled
<Story name="Disabled" args={{ disabled: true }}>
    {Template.bind({})}
</Story>

## Toggle
<Story name="Toggle" args={{ toggle: true }}>
    {Template.bind({})}
</Story>

## Slider
<Story name="Slider" args={{ slider: true }}>
    {Template.bind({})}
</Story>

### Available props and slots
<!-- https://storybook.js.org/docs/react/writing-docs/doc-blocks#customizing -->
<ArgsTable story="Default" />

### Available events
| event | description |
| :--- | :--- |
| `@input` | default v-model input event, returns the current value
| `@onUnchecked` | event to hook just after the input is unchecked, returns the current value
| `@beforeChecked` | event to hook just before the input is checked, returns the current value (value before update)
| `@beforeUnchecked` | event to hook just before the input is unchecked, returns the current value (value before update)

### Available functions that you can call on the component
| function name | description |
| :--- | :--- |
| `exec(command)` | helper to call any option on checkbox. Example: `COMPONENT.exec('indeterminate')` Details: https://fomantic-ui.com/modules/checkbox.html#behavior

### Usage
```javascript
import { Checkbox } from '@pitcher/vue-sdk'

const myLabel = 'Check me'
const myBoolean = false
```

```html
<!-- Simple usage -->
<Checkbox v-model="myBoolean" :label="myLabel"/>

<!-- Usage with props -->
<Checkbox v-model="myBoolean" label="mylabel_directly_here" toggle />

<Checkbox v-model="myBoolean" slider />
```
